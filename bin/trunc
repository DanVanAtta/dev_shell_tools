#!/bin/bash

if [ -z "$1" ]; then
 echo "Error, no truncation token specified, required one arg."
 echo "Usage:  $(basename $0) <token>"
 echo "Example:  echo '123abc123' | $(basename $0) abc -> '123abc'" 
fi

read input
echo $input | sed "s/$1.*//g"
